# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-08-21T21:54:24.401Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  workflow_dispatch:
    inputs:
      README_API_KEY:
        required: true
        description: your API token on readme.com

jobs:
  rdme-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `docs` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: docs . --key=${{ github.event.inputs.README_API_KEY }} --version=0.1
